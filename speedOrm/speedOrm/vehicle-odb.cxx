// -*- C++ -*-
//
// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

#include "vehicle-odb.hxx"

#include <cassert>
#include <cstring>  // std::memcpy


#include <odb/mysql/traits.hxx>
#include <odb/mysql/database.hxx>
#include <odb/mysql/transaction.hxx>
#include <odb/mysql/connection.hxx>
#include <odb/mysql/statement.hxx>
#include <odb/mysql/statement-cache.hxx>
#include <odb/mysql/simple-object-statements.hxx>
#include <odb/mysql/container-statements.hxx>
#include <odb/mysql/exceptions.hxx>
#include <odb/mysql/simple-object-result.hxx>
#include <odb/mysql/enum.hxx>

namespace odb
{
  // Vehicle
  //

  struct access::object_traits_impl< ::Vehicle, id_mysql >::extra_statement_cache_type
  {
    extra_statement_cache_type (
      mysql::connection&,
      image_type&,
      id_image_type&,
      mysql::binding&,
      mysql::binding&)
    {
    }
  };

  access::object_traits_impl< ::Vehicle, id_mysql >::id_type
  access::object_traits_impl< ::Vehicle, id_mysql >::
  id (const id_image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_value (
        id,
        i.id_value,
        i.id_null);
    }

    return id;
  }

  access::object_traits_impl< ::Vehicle, id_mysql >::id_type
  access::object_traits_impl< ::Vehicle, id_mysql >::
  id (const image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_value (
        id,
        i.vehicle_id_value,
        i.vehicle_id_null);
    }

    return id;
  }

  bool access::object_traits_impl< ::Vehicle, id_mysql >::
  grow (image_type& i,
        my_bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // vehicle_id_
    //
    t[0UL] = 0;

    // registration_number_
    //
    if (t[1UL])
    {
      i.registration_number_value.capacity (i.registration_number_size);
      grew = true;
    }

    // vehicle_type_
    //
    if (t[2UL])
    {
      i.vehicle_type_value.capacity (i.vehicle_type_size);
      grew = true;
    }

    // make_
    //
    if (t[3UL])
    {
      i.make_value.capacity (i.make_size);
      grew = true;
    }

    // model_
    //
    if (t[4UL])
    {
      i.model_value.capacity (i.model_size);
      grew = true;
    }

    // technical_characteristics_
    //
    if (t[5UL])
    {
      i.technical_characteristics_value.capacity (i.technical_characteristics_size);
      grew = true;
    }

    // status_
    //
    if (t[6UL])
    {
      i.status_value.capacity (i.status_size);
      grew = true;
    }

    return grew;
  }

  void access::object_traits_impl< ::Vehicle, id_mysql >::
  bind (MYSQL_BIND* b,
        image_type& i,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    std::size_t n (0);

    // vehicle_id_
    //
    if (sk != statement_update)
    {
      b[n].buffer_type = MYSQL_TYPE_LONGLONG;
      b[n].is_unsigned = 1;
      b[n].buffer = &i.vehicle_id_value;
      b[n].is_null = &i.vehicle_id_null;
      n++;
    }

    // registration_number_
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.registration_number_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.registration_number_value.capacity ());
    b[n].length = &i.registration_number_size;
    b[n].is_null = &i.registration_number_null;
    n++;

    // vehicle_type_
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.vehicle_type_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.vehicle_type_value.capacity ());
    b[n].length = &i.vehicle_type_size;
    b[n].is_null = &i.vehicle_type_null;
    n++;

    // make_
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.make_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.make_value.capacity ());
    b[n].length = &i.make_size;
    b[n].is_null = &i.make_null;
    n++;

    // model_
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.model_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.model_value.capacity ());
    b[n].length = &i.model_size;
    b[n].is_null = &i.model_null;
    n++;

    // technical_characteristics_
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.technical_characteristics_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.technical_characteristics_value.capacity ());
    b[n].length = &i.technical_characteristics_size;
    b[n].is_null = &i.technical_characteristics_null;
    n++;

    // status_
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.status_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.status_value.capacity ());
    b[n].length = &i.status_size;
    b[n].is_null = &i.status_null;
    n++;
  }

  void access::object_traits_impl< ::Vehicle, id_mysql >::
  bind (MYSQL_BIND* b, id_image_type& i)
  {
    std::size_t n (0);
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &i.id_value;
    b[n].is_null = &i.id_null;
  }

  bool access::object_traits_impl< ::Vehicle, id_mysql >::
  init (image_type& i,
        const object_type& o,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    bool grew (false);

    // vehicle_id_
    //
    if (sk == statement_insert)
    {
      long unsigned int const& v =
        o.vehicle_id_;

      bool is_null (false);
      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_image (
        i.vehicle_id_value, is_null, v);
      i.vehicle_id_null = is_null;
    }

    // registration_number_
    //
    {
      ::std::string const& v =
        o.registration_number_;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.registration_number_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.registration_number_value,
        size,
        is_null,
        v);
      i.registration_number_null = is_null;
      i.registration_number_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.registration_number_value.capacity ());
    }

    // vehicle_type_
    //
    {
      ::std::string const& v =
        o.vehicle_type_;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.vehicle_type_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.vehicle_type_value,
        size,
        is_null,
        v);
      i.vehicle_type_null = is_null;
      i.vehicle_type_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.vehicle_type_value.capacity ());
    }

    // make_
    //
    {
      ::std::string const& v =
        o.make_;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.make_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.make_value,
        size,
        is_null,
        v);
      i.make_null = is_null;
      i.make_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.make_value.capacity ());
    }

    // model_
    //
    {
      ::std::string const& v =
        o.model_;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.model_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.model_value,
        size,
        is_null,
        v);
      i.model_null = is_null;
      i.model_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.model_value.capacity ());
    }

    // technical_characteristics_
    //
    {
      ::std::string const& v =
        o.technical_characteristics_;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.technical_characteristics_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.technical_characteristics_value,
        size,
        is_null,
        v);
      i.technical_characteristics_null = is_null;
      i.technical_characteristics_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.technical_characteristics_value.capacity ());
    }

    // status_
    //
    {
      ::std::string const& v =
        o.status_;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.status_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.status_value,
        size,
        is_null,
        v);
      i.status_null = is_null;
      i.status_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.status_value.capacity ());
    }

    return grew;
  }

  void access::object_traits_impl< ::Vehicle, id_mysql >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // vehicle_id_
    //
    {
      long unsigned int& v =
        o.vehicle_id_;

      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_value (
        v,
        i.vehicle_id_value,
        i.vehicle_id_null);
    }

    // registration_number_
    //
    {
      ::std::string& v =
        o.registration_number_;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.registration_number_value,
        i.registration_number_size,
        i.registration_number_null);
    }

    // vehicle_type_
    //
    {
      ::std::string& v =
        o.vehicle_type_;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.vehicle_type_value,
        i.vehicle_type_size,
        i.vehicle_type_null);
    }

    // make_
    //
    {
      ::std::string& v =
        o.make_;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.make_value,
        i.make_size,
        i.make_null);
    }

    // model_
    //
    {
      ::std::string& v =
        o.model_;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.model_value,
        i.model_size,
        i.model_null);
    }

    // technical_characteristics_
    //
    {
      ::std::string& v =
        o.technical_characteristics_;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.technical_characteristics_value,
        i.technical_characteristics_size,
        i.technical_characteristics_null);
    }

    // status_
    //
    {
      ::std::string& v =
        o.status_;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.status_value,
        i.status_size,
        i.status_null);
    }
  }

  void access::object_traits_impl< ::Vehicle, id_mysql >::
  init (id_image_type& i, const id_type& id)
  {
    {
      bool is_null (false);
      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_image (
        i.id_value, is_null, id);
      i.id_null = is_null;
    }
  }

  const char access::object_traits_impl< ::Vehicle, id_mysql >::persist_statement[] =
  "INSERT INTO `vehicle` "
  "(`vehicle_id`, "
  "`registration_number`, "
  "`vehicle_type`, "
  "`make`, "
  "`model`, "
  "`technical_characteristics`, "
  "`status`) "
  "VALUES "
  "(?, ?, ?, ?, ?, ?, ?)";

  const char access::object_traits_impl< ::Vehicle, id_mysql >::find_statement[] =
  "SELECT "
  "`vehicle`.`vehicle_id`, "
  "`vehicle`.`registration_number`, "
  "`vehicle`.`vehicle_type`, "
  "`vehicle`.`make`, "
  "`vehicle`.`model`, "
  "`vehicle`.`technical_characteristics`, "
  "`vehicle`.`status` "
  "FROM `vehicle` "
  "WHERE `vehicle`.`vehicle_id`=?";

  const char access::object_traits_impl< ::Vehicle, id_mysql >::update_statement[] =
  "UPDATE `vehicle` "
  "SET "
  "`registration_number`=?, "
  "`vehicle_type`=?, "
  "`make`=?, "
  "`model`=?, "
  "`technical_characteristics`=?, "
  "`status`=? "
  "WHERE `vehicle_id`=?";

  const char access::object_traits_impl< ::Vehicle, id_mysql >::erase_statement[] =
  "DELETE FROM `vehicle` "
  "WHERE `vehicle_id`=?";

  const char access::object_traits_impl< ::Vehicle, id_mysql >::query_statement[] =
  "SELECT "
  "`vehicle`.`vehicle_id`, "
  "`vehicle`.`registration_number`, "
  "`vehicle`.`vehicle_type`, "
  "`vehicle`.`make`, "
  "`vehicle`.`model`, "
  "`vehicle`.`technical_characteristics`, "
  "`vehicle`.`status` "
  "FROM `vehicle`";

  const char access::object_traits_impl< ::Vehicle, id_mysql >::erase_query_statement[] =
  "DELETE FROM `vehicle`";

  const char access::object_traits_impl< ::Vehicle, id_mysql >::table_name[] =
  "`vehicle`";

  void access::object_traits_impl< ::Vehicle, id_mysql >::
  persist (database& db, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection (db));
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    im.vehicle_id_value = 0;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    {
      id_image_type& i (sts.id_image ());
      binding& b (sts.id_image_binding ());
      if (i.version != sts.id_image_version () || b.version == 0)
      {
        bind (b.bind, i);
        sts.id_image_version (i.version);
        b.version++;
      }
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    obj.vehicle_id_ = id (sts.id_image ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::Vehicle, id_mysql >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;
    using mysql::update_statement;

    callback (db, obj, callback_event::pre_update);

    mysql::transaction& tr (mysql::transaction::current ());
    mysql::connection& conn (tr.connection (db));
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& idi (sts.id_image ());
    init (idi, id (obj));

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::Vehicle, id_mysql >::
  erase (database& db, const id_type& id)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection (db));
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::Vehicle, id_mysql >::pointer_type
  access::object_traits_impl< ::Vehicle, id_mysql >::
  find (database& db, const id_type& id)
  {
    using namespace mysql;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    mysql::connection& conn (
      mysql::transaction::current ().connection (db));
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::Vehicle, id_mysql >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection (db));
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);
    assert (l.locked ()) /* Must be a top-level call. */;

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::Vehicle, id_mysql >::
  reload (database& db, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection (db));
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);
    assert (l.locked ()) /* Must be a top-level call. */;

    const id_type& id (object_traits_impl::id (obj));
    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::Vehicle, id_mysql >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace mysql;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  result< access::object_traits_impl< ::Vehicle, id_mysql >::object_type >
  access::object_traits_impl< ::Vehicle, id_mysql >::
  query (database& db, const query_base_type& q)
  {
    using namespace mysql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    mysql::connection& conn (
      mysql::transaction::current ().connection (db));

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += " ";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        text,
        false,
        true,
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) mysql::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::Vehicle, id_mysql >::
  erase_query (database& db, const query_base_type& q)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection (db));

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      text,
      q.parameters_binding ());

    return st.execute ();
  }
}

#include <odb/post.hxx>
